# 개발 계획서 — 나의 꿈 4트

**작성일**: 2026-08-14 (중간 체크포인트)
**엔진**: Unity 6000.4.5f1 / **씬**: `Assets/Scenes/MainGame.unity`
**현재 상태**: 빌드·플레이 가능. 승리/패배 양쪽 경로 검증 완료.

## 작업 순서

```
1. 게임 순환구조 (완료 및 재점검)
2. 게임 시스템          ← 지금 여기
3. UI                   (시스템 구현 시 필요하면 같이 제작)
4. 세부 디테일          (뼈대 완성 후 살 붙이기)
5. 그래픽 작업
```

## 중요도 기준

| 등급 | 의미 |
|---|---|
| **1** | 없으면 게임이 성립하지 않거나 다음 작업이 막힘 |
| **2** | 게임은 돌지만 재미·완성도에 큰 구멍 |
| **3** | 있으면 좋음. 뒤로 미뤄도 무방 |

---

# 완료된 것

## 순환구조
- 메인 메뉴 → 런 시작 → 보드/전투 → 런 종료 → 보상 화면 → 메인 메뉴 복귀
- 승리 경로: 보스 인카운터 → 처치 → 스테이지 1→2→3 → 마왕 처치 → 런 승리
- 패배 경로: HP 0 → 런 종료
- 메타 포인트 누적·저장 (PlayerPrefs)

## 보드 페이즈
- **분기 그래프 맵** (본선 루프 + 합류 우회로, 막다른 길 없음)
- **보스 AI**: 주사위 이동 + 경로 가중치 **총합** 비교 + **우선 가중치** 절대 우선
- **패닉 추격** (턴 경과·HP 임계값)
- **플레이어 교차로 선택** (맵 위 화살표 클릭)
- **보드 카드 페이즈** — 주사위 굴리기 전 카드 사용, 보드 카드 7종 전부 작동
- 노드 전 종류 반응: 몬스터·정예·이벤트·상점·월급·휴식·보물·저주
- 맵 시각화 (노드·간선·교차로 강조)

## 전투
- 4슬롯 포지션 배치, 도발·사용가능 슬롯·타겟 판정
- 카드 효과 7종 (피해·회복·방어막·상태이상·포지션이동·드로우·에너지)
- 상태이상 8종 (출혈·독·화상·기절·도발·약화·취약)
- **자기 대상 카드** (`targetSelf`)
- **포지션 직접 이동** — 화살표 + AP 1, 자리 교체, 고정포대(`isImmobile`) 지원
- **피격(TakeDamage) / 상실(LoseHp) 구분**
- 렐릭 트리거 6종 전부 연결
- 전투 보상 (골드 · 카드 3택1 · 렐릭)

## 기타
- 한글 폰트 (Neo둥근모 비트맵 TMP, 글리프 11,350자)
- Windows 빌드 정상

---

# 1. 게임 순환구조 — 재점검

> 순환 자체는 닫혔다. 남은 건 **스테이지를 넘나들 때 상태가 올바르게 유지/초기화되는지**다.

### 중요도 1
- [ ] **스테이지 전환 시 잔존 상태 전수 점검**
  골드·`isBossEncounter`는 수정 완료. 아직 미검증: 동료 HP, 전투 덱 상태(버린 더미),
  보드 카드 덱, 획득한 렐릭, 상태이상 잔존 여부
- [ ] **런 재시작 초기화 검증**
  `StartRun`에 `ResetRunResources` + `RelicManager.ClearAll`을 넣었지만 실플레이 미검증.
  메인 메뉴 → 새 런에서 이전 런의 렐릭·골드·덱이 남지 않는지 확인

### 중요도 2
- [ ] **노드 재배치 시스템** — GDD 3장. N회 루프 완주 시 비고정 노드 재배치.
  `NodeData.isFixed`는 이미 있고(시작 칸·교차로), 재배치 로직만 없음
- [ ] **특수승리(예능 루트) 조건 확정** — GDD 5장 미결정 상태

### 중요도 3
- [ ] 보드 한 바퀴 완주 보너스 여부 결정 (GDD 미결정)

---

# 2. 게임 시스템

> 지금 단계. 뼈대를 채우는 구간.

### 중요도 1
- [x] **동료 자동 행동 (B안)** — 완료 (실플레이 검증)
  `ActionPatternData`(SO)가 "언제(`TriggerTiming`) + 누구를(`TargetMode`) + 무엇을(기존 `CardEffect`)"를 담는다.
  동료·적이 같은 실행 엔진을 쓰고 차이는 호출 지점에만 있다.
  · GDD "동료 카드가 손패에 합산" 항목 **폐기 완료**. `CompanionData.combatCards`는 사용처 0건이라 남겨만 둠
  · 코스트 없음, 슬롯 번호 순 발동, 쿨다운·사용횟수는 전투마다 초기화(HP는 유지)
- [x] **적 AI를 같은 패턴 시스템으로 교체** — 완료.
  패턴이 없는 적은 기존 "최전방 때리기"로 폴백해 회귀가 없다
- [x] **적 다양성** — 광역 · 도발 · 후열 저격 · 부활 · 자폭 전부 구현·검증.
  하수인 5종에 패턴 연결, `Minion_폭발슬라임` 신규
- [x] **적 의도(인텐트) 시스템** — 완료 (표시 포함).
  패턴을 라운드 시작에 미리 정해 `CombatEntity.CurrentIntent`에 보관하고,
  타겟은 실행 순간에 재계산한다 → 슬롯 이동으로 회피가 성립.
  표시값은 슬롯이 바뀔 때마다 `RefreshIntentPreviews()`가 다시 채우므로 화면이 실제와 어긋나지 않는다
- [ ] **보드 페이즈 편성창**
  현재 `TryAddCompanion`이 영입 순서대로 빈 슬롯에 꽂을 뿐이라 `CompanionData.preferredSlot`이 미사용.
  `CombatManager.PlaceAllEntities`가 `entity.CurrentSlot`을 읽으므로 편성창은 그 값만 바꾸면 됨

### 중요도 2
- [ ] **메타 해금이 통째로 죽어 있음**
  · `UnlockData` 에셋 **0개** → `CheckUnlocks`가 항상 빈 결과
  · `GetUnlockedCards / GetUnlockedCharacters / GetUnlockedStartingRelics` **호출처 없음**
  → 메타 포인트는 쌓이는데 해금되는 게 아무것도 없다
- [ ] **`CharacterData.startingRelic` 미적용** — 필드는 있는데 `PlayerCharacter.Initialize`가 무시
- [ ] **보스 상점 선점 타이밍 버그**
  `BossAiBuyRelics()`가 `OpenShop()` 안에서 호출됨 → **플레이어가 상점을 열 때** 보스가 산다.
  GDD는 보스가 상점 칸에 착지했을 때 사는 구조 (`HandleBossLanded`는 현재 아무것도 안 함)
- [ ] **포션 시스템** — `ShopManager.TryBuyPotion`이 `DeckManager.AddCard`를 호출해
  포션이 그냥 덱에 섞인다. 전투 중 임의 사용하는 소비 아이템으로 분리 필요
- [ ] **등급(Rarity) 시스템**
  `Rarity { Common, Uncommon, Rare, Unique }` + 가중 추첨 + 등급별 가격.
  현재 상점·보상이 완전 무작위라 `타격`과 `강타`가 동일 확률.
  주의: 카드 테두리가 이미 직업색이라 등급까지 표현하려면 표기를 분리해야 함
- [~] **보스 페이즈 패턴** — 구조 완료, **에셋만 미작성**.
  패턴 SO의 `minPhase` / `requirePanic` / `hpBelowRatio`로 조건을 걸 수 있고
  `CombatManager.MeetsCondition`이 `CurrentPhase` / `IsPanicMode`를 읽는다.
  보스 3종의 `patterns`가 비어 있어 현재는 폴백(최전방 1타)으로 동작한다

### 중요도 3
- [ ] 슬롯 설치 아이템 (GDD 7장 — 빈 슬롯에 설치해 패시브 획득). `isImmobile` 기반으로 구현 가능
- [ ] 카드 업그레이드 (휴식 노드의 "카드 1장 업그레이드" — 현재 회복만)
- [ ] 소모(Exhaust) 카드 — `DeckManager.ExhaustCard`는 있는데 쓰는 카드가 없음
- [ ] `RelicEffectType.IncreaseMaxHp` / `EventEffectType.MaxHpChange` 미구현 (`CombatStats.AdjustMaxHp` 필요)
- [ ] 보스 Passive 렐릭 미적용 (`RelicManager.ApplyPassives`가 플레이어만 처리)
- [ ] 캐릭터 2종 추가 (마법사·도적) — 현재 전사 1종
- [ ] 저주 카드 에셋 (저주 노드가 카드 대신 HP만 깎는 중)

---

# 3. UI

> 시스템 구현 시 짝으로 만든다. 단독으로 앞서 만들지 않는다.

### 중요도 1
- [x] **적 의도 표시** — 완료. `CombatSlotGridUI`가 이동 화살표와 같은 방식으로 런타임 생성한다.
  적 슬롯 = 무엇을 할지(종류 색 + 수치) / 플레이어 슬롯 = 그 칸이 받을 피해 합계.
  **아트는 임시 색 도형**이라 아이콘 에셋이 나오면 `IntentColor`/`IntentLabel`만 교체하면 된다
  (※ `EnemyIntent.preview*`는 표시 전용이다. 실행 로직이 읽으면 슬롯 이동 회피가 깨진다)
- [ ] **상태이상 표시** — `SlotUI`가 HP·방어막만 표시. 출혈/독/도발/취약이 화면에 안 보임
- [ ] **편성창 UI** — 시스템과 세트

### 중요도 2
- [ ] **순간이동 목적지 선택 UI** — `BoardTeleport`의 `value = -1`(UI 선택)이 미구현이라 순간이동 카드가 반쪽
- [ ] **보스 정보 패널** — GDD 14장: 보스 HP·공격력·골드·하수인 수를 보드에서 확인
- [ ] 덱 보기 / 버린 더미 보기

### 중요도 3
- [ ] 카드 상세 팝업 개선
- [ ] 설정 화면 (볼륨·해상도)

---

# 4. 세부 디테일

> 전체 뼈대가 선 뒤에 붙인다.

### 중요도 1
- [ ] **보드 이동 연출** — 현재 순간이동. 칸 단위로 지나가야 "6칸 갔다"는 감각이 생김
- [ ] **보스 턴 가시화** — 보스 주사위·이동 경로·하수인 획득이 전혀 안 보임

### 중요도 2
- [ ] 전투 연출 — 피격·사망 이펙트, 행동 간 딜레이
  (`CombatManager.EnemyAct`에 `yield return null`만 있어 적 턴이 순식간에 끝남)
- [ ] **손패 부채꼴 + 호버 확대** (슬더스식 부드러운 애니메이션)
  `HandUI.rotateCards`가 false로 꺼져 있음. 회전은 픽셀 글자를 뭉개니 라벨 처리 주의
- [ ] **밸런싱** — 피해 공식, 상태이상 수치, 골드·가격, 보스 HP, 패닉 임계값
- [ ] 보드 손패가 맵을 가리는 배치 문제 (아이소메트릭 전환으로 해소 예정)

### 중요도 3
- [ ] 사운드 (BGM·효과음)
- [ ] 화면 전환 페이드

---

# 5. 그래픽 작업

### 중요도 1
- [ ] **보드 시점 아이소메트릭(2.5D) 전환** — GDD 14장.
  다이아몬드 타일(2:1) 격자 위에 픽셀아트 말이 서고 UI는 하단 독킹.
  현재 UGUI 평면 원형 배치는 임시. `BoardMapView` 좌표 계산을 아이소 투영으로 교체
- [ ] **Neo둥근모 OFL 라이선스 파일 배포 빌드에 동봉** — SIL OFL 필수 조건 (배포 전 반드시)

### 중요도 2
- [ ] 카드 뒷면 스프라이트를 `CardUI`에 적용 (에셋은 `Assets/Art/Cards/`에 이미 존재)
- [ ] `Deck_Half` / `Deck_Quarter` 재생성 — Stitch 프롬프트의 "lying on a table" 탓에 탁자가 아트에 붙어 배경 제거 불가
- [ ] 캐릭터·동료·적 스프라이트 (현재 회색 사각형 플레이스홀더)
- [ ] 카드 아트워크

### 중요도 3
- [ ] 스테이지별 배경
- [ ] 타격·회복 이펙트
- [ ] 타이틀 화면 아트

---

# 부록 — 재발견하기 어려운 함정

작업 중 다시 밟기 쉬운 것들. 상세는 프로젝트 메모리 참고.

| 함정 | 요약 |
|---|---|
| TMP 비트맵 폰트 | 아틀라스 패딩 **≥ 1** 필수. 0이면 낱글자에도 유령 획 |
| Neo둥근모 | 크기 **16px 배수**, 볼드·이탤릭 금지. `※★♥→←—` 등 없음 |
| Unity Error Pause | 켜두면 Coplay 캡처 에러에 플레이가 멈춰 오진하게 됨 |
| Game 뷰 Scale | 미리보기 배율일 뿐 렌더 해상도 아님 |
| 프리팹 인스턴스 | 스크립트로 값 직접 대입 시 오버라이드 미기록 → 플레이 시 되돌아감 |
| `SlotMask.None` | "타겟 불필요"가 아니라 "타겟 가능 슬롯 0개" → 카드가 안 나감 |
| `SlotUI.IsTargetable` | 유효 타겟 검사와 클릭 게이트를 겸함. UI 가드만 믿지 말 것 |
| `Random.Range(int,int)` | min ≥ max면 min 반환 → 범위 초과 인덱스 |
| Coplay `execute_script` | 호출마다 새로 컴파일 → static 유지 안 됨 |
| 보드 UI 대기 지점 4곳 | 전투 게이트·교차로·보드 카드·보물. 전부 자동 통과 안전장치 있음 |
| 자원의 층위 | 골드·렐릭은 **런 단위**. 스테이지 단위 코드에서 초기화하면 증발 |

---

# 부록 — 확정된 설계 결정

- **전투 슬롯**: 플레이어 좌측 / 적 우측 (GDD 다이어그램과 반대지만 목업 채택)
- **동료**: 자동 행동 (B안 — Monster Train 모델)
- **행동력(AP)**: 화살표 직접 이동 전용. 카드 이동은 AP 무소모.
  `EffectType.MovePosition`은 타겟이 아니라 **시전자**를 이동
- **AP는 파티 공용** — 동료를 옮겨도 플레이어 AP 소모 (동료는 `maxActionPoints = 0`)
- **이동 판정 3분리**

  | 상태 | 스스로 이동 | 남이 밀어냄 | 전투 행동 |
  |---|---|---|---|
  | 정상 | O | O | O |
  | 기절(Stun) | **O** | O | X |
  | 쓰러짐(HP 0) | X | O | X |
  | 고정포대 | X | **X** | **O** |

  기절이 이동까지 막으면 슬롯 봉쇄가 과한 리스크라 **기절은 이동을 막지 않음**
- **용어**: HP 0 = **쓰러짐**, `StatusEffect.Stun` = **기절**
- **피격 vs 상실**: 저주·이벤트 HP 감소는 상실 → 방어막·취약·피격 트리거를 타지 않음 (슬더스 동일)
- **아트 디자인 시스템**: Stitch의 **Eldritch Chronicle** (양피지 + 던전 슬레이트 + 골드, 0px 샤프 코너)
